using Emp.Core.Entities.Concretes;
using Emp.Entity.Enums;

namespace Emp.Entity.Entities;

public class User : EntityBase
{
    public User()//string name, string lastName, string phone, string email, string department, string salary, DateTime dateTime, bool isDeleted, Role role)
    {
        // Name = name;
        // LastName = lastName;
        // PhoneNumber = phone;
        // Email = email;
        // Department = department;
        // Salary = salary;
        // DateOfEntry = dateTime;
        // IsDeleted = isDeleted;
        // RoleOfEmp = role;
    }
}